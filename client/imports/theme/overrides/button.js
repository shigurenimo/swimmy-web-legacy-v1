import { Meteor } from 'meteor/meteor'

export default {
  root: {
    marginTop: 0,
    marginBottom: 0,
    padding: '0px 10px',
    verticalAlign: 'top',
    color: Meteor.settings.public.color.primary,
    textTransform: 'default',
    letterSpacing: '1px'
  },
  dense: {}
}
